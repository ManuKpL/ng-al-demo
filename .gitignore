// prevent all node modules to be indexed by git
node_modules

// do not track files inside the dist directory
// except the .gitkeep file (prevents directory from being ignored)
dist/*
!dist/.gitkeep
